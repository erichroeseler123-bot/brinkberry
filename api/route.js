module.exports=async(req,res)=>{try{const u=new URL(req.url,'https://brinkberry.local'),fromLat=Number(u.searchParams.get('fromLat')),fromLon=Number(u.searchParams.get('fromLon')),toLat=Number(u.searchParams.get('toLat')),toLon=Number(u.searchParams.get('toLon'));if(![fromLat,fromLon,toLat,toLon].every(Number.isFinite))return res.status(400).json({error:'Coordinates required'});const url=`https://router.project-osrm.org/route/v1/driving/${fromLon},${fromLat};${toLon},${toLat}?overview=false&steps=false`;const r=await fetch(url,{headers:{'User-Agent':'Brinkberry/1.0 (https://brinkberry.com)'}});if(!r.ok)throw new Error(`OSRM ${r.status}`);const j=await r.json();if(j.code!=='Ok'||!j.routes?.[0])throw new Error(j.code||'No route');const x=j.routes[0];res.setHeader('Cache-Control','public, s-maxage=300');res.status(200).json({durationMinutes:Math.ceil(x.duration/60),distanceMiles:Math.round((x.distance/1609.344)*10)/10,provider:'OSRM'})}catch(e){console.error(e);res.status(503).json({error:'Travel estimate unavailable'})}};
