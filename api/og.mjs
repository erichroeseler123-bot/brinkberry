import React from 'react';
import { ImageResponse } from '@vercel/og';
const SUPABASE_URL='https://onsnxawujlzfrzhwndyu.supabase.co';
const KEY='sb_publishable_2ygc158CkPm28E9j6zNdmA_Cvvj5kGr';
async function getEvent(id){const r=await fetch(`${SUPABASE_URL}/rest/v1/rpc/bb_get_public_event`,{method:'POST',headers:{apikey:KEY,authorization:`Bearer ${KEY}`,'content-type':'application/json'},body:JSON.stringify({p_id:id})});if(!r.ok)throw new Error(`Supabase ${r.status}`);const rows=await r.json();return rows?.[0]||null}
export default async function handler(req){const u=new URL(req.url,'https://brinkberry.local'),id=u.searchParams.get('id');if(!id)return new Response('Missing id',{status:400});const e=await getEvent(id);if(!e)return new Response('Not found',{status:404});const price=e.price_status==='free'?'Free':(e.price_display||'Details'),when=new Date(e.start_time).toLocaleString('en-US',{timeZone:'America/Denver',weekday:'long',month:'short',day:'numeric',hour:'numeric',minute:'2-digit'});return new ImageResponse(React.createElement('div',{style:{width:'100%',height:'100%',display:'flex',flexDirection:'column',justifyContent:'space-between',background:'#080610',color:'#f4eff8',padding:'64px',fontFamily:'sans-serif'}},React.createElement('div',{style:{fontSize:34,fontWeight:900,color:'#ffb86b'}},'● Brinkberry'),React.createElement('div',{style:{display:'flex',flexDirection:'column',gap:'20px'}},React.createElement('div',{style:{fontSize:68,fontWeight:900,lineHeight:1.05}},e.title),React.createElement('div',{style:{fontSize:30,color:'#c5bbd0'}},[e.venue_name,when,price,e.vibe_labels?.[0]].filter(Boolean).join(' · '))),React.createElement('div',{style:{fontSize:28,color:'#90869e'}},'What’s happening near you right now?')),{width:1200,height:630})}
